"use client";

import { Button } from "@/components/ui/button";

export const SignInButton = () => {
  // TODO: add signIn function from next-auth
  return (
    <Button variant="secondary" size="sm" onClick={() => {}}>
      Sign In
    </Button>
  );
};
